/**
 * Represents an asset to be loaded.
 */
class AssetLoader {
    
    /**
     * Creates an AssetLoader instance.
     */
    constructor() {}
    
    /**
     * True if the asset has been loaded.
     */
    isLoaded() {}
}
