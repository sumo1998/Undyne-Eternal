/**
 * Represents a graphically-drawn object.
 */
class GraphicsObject {
    
    /**
     * Updates the graphics object based on the change in time.
     * @param deltaMs The change in milliseconds since the last update
     */
    update(deltaMs) {}
    
    /**
     * Renders the graphics object.
     */
    render() {}
}
